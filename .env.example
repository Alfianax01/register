# =============================================================================
# KONFIGURASI ENVIRONMENT SISTEM REGISTRASI RAPIM TNI 2026
# =============================================================================

# Server & Application URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_URL=http://localhost:3000
NODE_ENV=development

# -----------------------------------------------------------------------------
# DATABASE MYSQL (Vercel Serverless / PlanetScale / Railway / TiDB / Aiven)
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_NAME=tni_rapim_2026
DB_USER=root
DB_PASSWORD=
DB_SSL=false

# -----------------------------------------------------------------------------
# EMAIL NOTIFIKASI OTOMATIS (NODEMAILER SMTP)
# EMAIL NOTIFIKASI OTOMATIS (NODEMAILER SMTP / RESEND)
# -----------------------------------------------------------------------------
SMTP_HOST=
# Sakelar Pengiriman Email:
# - false = SIMULASI (mencegah koneksi SMTP keluar & spam failure pada dev/testing)
# - true  = KIRIM NYATA (gunakan di Vercel Production dengan kredensial valid)
EMAIL_ENABLED=false

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_USER=eregister287@gmail.com
SMTP_PASS=
SMTP_FROM_NAME="Panitia RAPIM TNI 2026"
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="Portal RAPIM TNI 2026"
SMTP_FROM_EMAIL=eregister287@gmail.com

# Opsional: Resend API Key (HTTPS Port 443, rekomendasi untuk Vercel Serverless)
RESEND_API_KEY=
RESEND_FROM=eregister287@gmail.com

# -----------------------------------------------------------------------------
# KEAMANAN SESI & TOKENS (JWT & AUTH)
# -----------------------------------------------------------------------------
JWT_SECRET=super-secret-tni-rapim-encryption-key-2026


